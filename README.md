# BD_navegador_de_tabelas
Navegador de tabelas, usando DatabaseMetaData, que a partir de uma conexão de banco de dados (MySQL ou PostgreSQL), mostra os nomes de tabelas, e os nomes e tipos dos campos.
